name: Linting

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  lint:
    uses: cap-js/.github/.github/workflows/lint.yml@ci/addWorkflows
    secrets: inherit
